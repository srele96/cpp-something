# Prototype

A collection of prototype scripts.

## Docs

For fast prototyping, computation, tests, scripting, install [`uv`](https://github.com/astral-sh/uv).

Then, you can run scripts, like:

```bash
uv run prototype/clusters.py
```
